import { MonthDays } from '../entities/monthEntities';

// There are just 6 days because the first day on weeb is 0 (Sunday)
export const DAYS_ON_WEEK = 6;

export const DAYS = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];

export const GENERATED_DAYS: MonthDays = {};
